{% extends 'base.html.twig' %}
{% block body %}
    {{ form_start(form) }}
        {{ form_row(form.profilePicture) }}
        <button type="submit" class="btn">valider</button>
    {{ form_end(form) }}
{% endblock %}