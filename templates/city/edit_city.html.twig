{% extends 'base.html.twig' %}

{% block title %}Edit City{% endblock %}

{% block body %}
    <div class="container">
        <h1>Editer une ville</h1>
        <div class="card blue-grey darken-1">
            <div class="card-content white-text">
                {{ include('city/_form.html.twig', {'button_label': 'Mise à jour'}) }}
            </div>
        </div>
        <a class="btn" href="{{ path('main_cities') }}">Retour à la liste des villes</a>
    </div>

    </div>
    </br>

{% endblock %}
