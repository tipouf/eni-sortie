{% extends 'base.html.twig' %}

{% block title %}Edit Contributor{% endblock %}

{% block body %}
    <div class="container">
        <div class="card light-blue darken-4">
            <div class="card-content row">
                <h1 class="white-text">Edition du profil</h1>
                <div>
                    {{ include('profile/_form.html.twig', {'button_label': 'Mise à jour'}) }}
                </div>
            </div>
            <div style="padding-bottom: 20px; padding-left: 20px">
                <a class="btn" href="{{ path('app_home') }}">Annuler</a>
            </div>
        </div>
    </div>
{% endblock %}
